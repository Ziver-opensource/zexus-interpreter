# Test current Zexus features
let x = 10
let y = 20
let sum = x + y
print "Sum: " + string(sum)

# Test lists
let numbers = [1, 2, 3, 4, 5]
print "First: " + string(first(numbers))
print "Last: " + string(last(numbers))
print "Length: " + string(len(numbers))

# Test actions (functions)
action multiply(a, b) {
    return a * b
}
let result = multiply(5, 6)
print "Multiplication: " + string(result)
EOF
